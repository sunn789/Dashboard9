#this is a radis config file for p;oad to the remot server for config that radis in my docker is configured with this setting
#scp -P 9011 D:\path\to\redis.conf root@13.123.13.13:/home/user/redis.conf
# Allow connections from external networks (not just localhost)
bind 0.0.0.0

# Disable protected mode (for external access)
protected-mode no

# Optional: Set a password for Redis (recommended)
requirepass your_strong_password_here

# Append other Redis configurations as needed (e.g., persistence, logging)